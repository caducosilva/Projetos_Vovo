# Dependencias e builds
node_modules/
dist/
dist-ssr/
build/
.gradle/
*.tsbuildinfo

# Android / Capacitor gerados
local.properties
android/app/src/main/assets/public/
android/app/build/
android/build/
android/capacitor-cordova-android-plugins/
*.apk
*.aab
*.keystore
*.jks

# Python
__pycache__/
*.pyc

# Segredos
*.env
.env.local
credentials*.json
*token*.json
*api*key*.json
cookies/

# Streams das cameras, gerados em tempo real.
# Recortado na pasta de proposito: "*.ts" solto engolia todo arquivo
# TypeScript do projeto, e os dois apps ficaram sem vite.config.ts,
# sem capacitor.config.ts e sem a pasta src/utils inteira dentro do repo.
stream/
**/stream/*.ts
**/stream/*.m3u8

# Logs e temporarios
*.log
*.tmp
err.txt

# Editor / sistema
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
*.suo
*.sw?

# APKs prontos (binarios de terceiros, centenas de MB)
APKs_Prontos/

# Visualizador de cameras vive no repositorio privado CameraLocal
cameras-vovo/

# Capturas de tela de depuracao soltas na raiz
/*.png

# Capturas tiradas durante o desenvolvimento (phone_screen_radio_test1.png e
# companhia). Sao 8 MB de tela antiga, de um design que ja foi substituido, e
# entram no repo so para envelhecer. Ficam no disco, fora do historico.
docs/screenshots/
